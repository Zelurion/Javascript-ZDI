function sum(x, y) {
    let ergebnis = x + y;
    return ergebnis;
}

sum(10, 2);
console.log("Ergebnis = " + sum(2, 3));
/*
AUFGABE:
1) Funktion anlegen für Subtraktion oder Multiplikation oder Division
2) Aufruf dieser Funktion
*/

function kreisfläche(π,r)
{
    let ergebnis2 = π * (r * 2);
    return ergebnis2;
}

kreisfläche(10, 2);
console.log("Die Kreisfäche beträgt: " + kreisfläche(3.14,5) + "m^2");